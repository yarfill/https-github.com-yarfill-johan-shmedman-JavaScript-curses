'use strict';// Temporarily disabled for debugging. So if you has a mistake in javascript this mode will help you to found it.

let hasDriversLicence = false;
const passTest = true;

if (passTest) hasDriverLicence = true; // !It's a mistake for mode 'strict'!
if (hasDriversLicence) console.log('I can drive');

// const interface = 'Audio';
// const private = 534;